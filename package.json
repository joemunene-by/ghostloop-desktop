{
  "name": "ghostloop-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Native desktop control panel for ghostloop. Tauri + Rust shell + Next.js frontend + sidecar Python runtime.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0"
  }
}
